const Reflux = require('reflux');

const MworldActions = Reflux.createActions([
  /**
   * define your actions as strings below, for example:
   */
  'toggleStatus'
]);

module.exports = MworldActions;
